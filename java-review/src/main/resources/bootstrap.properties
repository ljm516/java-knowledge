spring.application.name=virgo
server.port=${PORT}
eureka.client.service-url.defaultZone=http://localhost:8080/eureka/

# 本地启的 config-server
spring.cloud.config.profile=${ENV}
spring.cloud.config.label=master
spring.cloud.config.uri=${CONFIG_URL}
